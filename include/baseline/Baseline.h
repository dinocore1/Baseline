#ifndef BASELINE_H_
#define BASELINE_H_

#include <cstddef>
#include <cstdint>

#ifndef MIN
  #define MIN(a, b) ((a) < (b) ? (a) : (b))
#endif

#ifndef MAX
  #define MAX(a, b) ((a) > (b) ? (a) : (b))
#endif


#endif // BASELINE_H_